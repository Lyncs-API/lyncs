"""
This package is part of lyncs.
This list of configuration options is automatically generated from 
${CMAKE_SOURCE_DIR}/lyncs-config/__init__.py.in.
Change that file to add more options.
"""

# Information about installation
version = "${LYNCS_VERSION}"
source_dir = "${CMAKE_SOURCE_DIR}"
bynary_dir = "${CMAKE_BINARY_DIR}"
install_dir = "${CMAKE_INSTALL_PREFIX}"
build_type = "${CMAKE_BUILD_TYPE}"

# Information about quda
with_quda = "${ENABLE_QUDA}"
